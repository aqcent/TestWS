@using TestWS.Models.Tickets
@model TestWS.Models.Tickets.TimeSlot

@section SubNav
{
    @Html.Partial("~/Views/Shared/SubNavigation.cshtml", SubNavigationMenu.TimeSlots)
}

<h2>Edit TimeSlot</h2>

@using (Html.BeginForm("EditTimeSlot", "TicketsAdmin"))
{
    @Html.HiddenFor(x => x.Id)

    <div class="form-group">
        <div class='input-group date' id='@Html.IdFor(x=>x.StartTime)'>
            <span class="input-group-addon">
                <span class="glyphicon glyphicon-calendar"></span>
            </span>
            <input type='text' name='@Html.NameFor(x=>x.StartTime)' class="form-control" value="@Model.StartTime"/>
        </div>
    </div>

    @Html.Partial("~/Views/Editors/MoviesList.cshtml", Model.MovieID)
    @Html.Partial("~/Views/Editors/HallsList.cshtml", Model.HallID)
    @Html.Partial("~/Views/Editors/TariffsList.cshtml", Model.TariffID)

    <input class="btn btn-success" type="submit" value="Save changes" />
}

@section scripts
{
    <script type="text/javascript">
        $(function () {
            $('#@Html.IdFor(x=>x.StartTime)').datetimepicker({
                format: 'DD.MM.YYYY HH:mm'               
            });
        });
    </script>
}

